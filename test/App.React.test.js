
/*import​ React ​from​ '​react'​;
import​ App ​from​ ​'./src/App'​;
import​ renderer ​from​ ​'react-test-renderer';​

test(​'renders correctly'​, () => {
    //const​ tree = renderer.create(​<​App />​).toJSON();
    //expect(tree).toMatchSnapshot();
}); 


const api = {
    key: process.env.REACT_APP_OPENWEATHERMAP_API_KEY, // End server session before using key. Because the env. file wont be read.
    base: "https://api.openweathermap.org/data/2.5/" //URL.
    query: 'Pretoria' // Default query.
}

const​ sum = ​require​(​'./src/App'​);
    test(​'if fetch actually fetches the correctdata'​, () => {
        expect(fetch(​`${api.base}weather?q=${api.query}&units=metric&appid=${api.key}`).toBe(​)​}

test('if fetch actually fetches the correctdata', () => {
    return fetchData().then(data => {
    expect(data).toBe('');
    });
}); 

*/